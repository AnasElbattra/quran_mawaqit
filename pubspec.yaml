name: quran_mawaqit
description: "Quran as text"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.4.3
dependencies:
  flutter:
    sdk: flutter
  sizer: ^3.0.5
  archive: ^4.0.5
  path_provider: ^2.1.5
  preload_page_view: ^0.2.0
  http: ^1.3.0
  quran:
    path: ./packages/quran-1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/

  fonts:
    - family: hafs
      fonts:
        - asset: assets/fonts/uthmanic_hafs_v22.ttf
          weight: 500
    - family: UthmanicHafs13
      fonts:
        - asset: assets/fonts/UthmanicHafs13.otf
